using System;

namespace src {
  public class Translator {
    // TODO: arreglen esto: 
    // translator.cs(7,28): error CS0266: Cannot implicitly convert type 'double' to 'float'. An explicit conversion exists (are you missing a cast?) 
    // translator.cs(11,25): error CS0266: Cannot implicitly convert type 'double' to 'float'. An explicit conversion exists (are you missing a cast?)
    // public static void Translate3DMToPartition(_3DM original_problem) {
    //   uint sizeM = original_problem.GetMSize(); // nº de tripletas de M (k)
    //   float numberOfBits = Math.Log(sizeM + 1.0f, 2); // nº de bits por elemento (p)
    //   if (numberOfBits % 1 == 0.0) {
    //     numberOfBits += 1;
    //   } else {
    //     numberOfBits =  Math.Ceiling(numberOfBits);
    //   }
    //   uint sizeXYZ = original_problem.GetXYZSize();

    // }
  }
}
